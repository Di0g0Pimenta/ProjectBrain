# Vision

ProjectBrain aims to become a persistent memory layer for software projects.

Instead of forcing an LLM to repeatedly analyze an entire repository, ProjectBrain incrementally builds and maintains a structured knowledge graph that can be queried efficiently.

The long-term goal is to enable local LLMs to reason about projects of virtually unlimited size while minimizing context usage.